
rotation_step = 10  # Degrees to rotate when turning the camera left/right
upper_bound = 145   # Max camera angle
lower_bound = 35    # Min camera angle

# image_width = 1280

num_slots = 9       # Should always be odd, so we can clearly define a center slot
